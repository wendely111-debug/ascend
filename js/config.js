// Preencha com os dados do seu projeto Supabase (Project Settings > API).
// A "anon key" é pública por design: quem protege os dados é o RLS do schema.sql.
// Deixe em branco para rodar no MODO SOLO (offline, dados só neste aparelho).
export const CONFIG = {
  SUPABASE_URL: '',
  SUPABASE_ANON_KEY: '',
};
